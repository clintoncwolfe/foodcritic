#!/usr/bin/env ruby
require_relative '../lib/foodcritic'
module FoodCritic
  cmd_line = CommandLine.new(ARGV)
  review, status = Linter.run(cmd_line)
  OutputManager.new(cmd_line.options).output_all(review)
  exit status.to_i
end
